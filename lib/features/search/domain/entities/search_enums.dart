/// Search type enumeration for specialized searches
enum SearchType { general, news, scholar, shopping, images, videos }

/// Recency filter for time-based searches
enum SearchRecency { any, day, week, month, year }
